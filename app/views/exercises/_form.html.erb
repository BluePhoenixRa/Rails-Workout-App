<%= form_for([current_user, @exercise], :html => { multipart: true }) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error-explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@exercuse.errors.count, "error") %> prohibited this restaurant from being saved:</h4>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :duration %><br>
      <%= f.text_field :duration, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :workout %><br>
      <%= f.text_area :workout, class: "form-control"%>
  </div>
  <div class="form-group">
    <%= f.label :workout_date, "Date" %><br>
      <%= f.text_field :workout_date, class: "form-control"%>
  </div>
  <div class="actions">
      <%= f.submit "Add Workout", class: "btn btn-primary" %>
  </div>
<% end %>
